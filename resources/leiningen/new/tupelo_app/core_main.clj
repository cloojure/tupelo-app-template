(ns {{name}}.core
  "Contains the core functions for namespace `{{name}}.core`."
  (:use tupelo.core)
  (:require
    [clojure.string :as str]
    [schema.core :as s]
  ))

(defn -main []
  (println "main - enter")
  )

