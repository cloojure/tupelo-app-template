(ns {{name}}.core
  "Contains the core functions for namespace `{{name}}.core`."
  (:use tupelo.core)
  (:require
    [clojure.string :as str]
    [tupelo.schema :as tsk]
    [tupelo.string :as ts] ))

(defn -main []
  (println "main - enter")
  )

