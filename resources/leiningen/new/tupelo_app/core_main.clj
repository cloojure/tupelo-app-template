(ns {{name}}.core
  "Contains the core functions for namespace `{{name}}.core`."
  (:use tupelo.core)
  (:require
    [clojure.string :as str]
    [plumatic.schema :as s]
  ))

(defn -main []
  (println "main - enter")
  )

